import { px } from '@utils/responsive';

export const bottomTabSize = {
  height: px(80),
};

// 화면 공통 padding
export const screenPadding = {
  top: px(8),
  horizontal: px(20),
};

// 이미지 분석 시 적용되는 이미지 크기
export const targetImageSize = {
  width: 768,
  height: 768,
};
