import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollViewWrapper: {
    backgroundColor: '#fff',
    flex: 1,
    paddingHorizontal: 5,
    paddingTop: 12,
    paddingBottom: 20,
  },
});
